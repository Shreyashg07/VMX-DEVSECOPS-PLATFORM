version: "3.8"
services:
  api:
    build:
      context: ./api
    ports:
      - "8000:8000"
    volumes:
      - ./integrity.db:/app/integrity.db
    environment:
      - INTEGRITY_DB_PATH=sqlite:///./integrity.db
